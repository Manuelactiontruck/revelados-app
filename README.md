# REVELADOS

PWA privada de trivial íntimo para dos, con Supabase (base de datos + notificaciones push reales) y despliegue en Vercel.

## Desarrollo local
```
npm install
npm run dev
```

## Producción
Importado en Vercel desde este repositorio (build command `npm run build`, output `dist`).
